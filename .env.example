# ============================================================================
# VARIABLES DE ENTORNO - RIFA SILVERADO Z71 2024
# IMPORTANTE: Configurar tanto en archivo .env.local como en Netlify UI
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (REQUERIDAS)
# ============================================================================
# Obtén estos valores de tu proyecto en https://supabase.com > Settings > API

# URL del proyecto Supabase - SIN trailing slash
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Anon/Public Key - JWT token que empieza con 'eyJ'
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-clave-anonima-aqui

# Service Role Key (opcional - solo para operaciones admin del servidor)
SUPABASE_SERVICE_ROLE_KEY=tu-clave-de-servicio-aqui

# JWT Secret (opcional pero recomendado para validación)
SUPABASE_JWT_SECRET=tu-jwt-secret-aqui

# ============================================================================
# BUILD & DEPLOYMENT
# ============================================================================
# Deshabilitar telemetría de Next.js
NEXT_TELEMETRY_DISABLED=1

# Configuración de entorno
NODE_ENV=development

# Optimizaciones para producción
NEXT_OPTIMIZE_FONTS=true
NEXT_OPTIMIZE_IMAGES=true

# ============================================================================
# ANALYTICS (OPCIONAL)
# ============================================================================
# Google Analytics ID (cuando lo configures)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Hotjar ID (cuando lo configures)  
# NEXT_PUBLIC_HOTJAR_ID=XXXXXXXXX

# ============================================================================
# CONFIGURACIÓN DE DOMINIO (PRODUCCIÓN)
# ============================================================================
# URL del sitio en producción
NEXT_PUBLIC_SITE_URL=https://tu-sitio.netlify.app

# API URL (normalmente la misma que SITE_URL)
NEXT_PUBLIC_API_URL=https://tu-sitio.netlify.app/api

# ============================================================================
# INSTRUCCIONES PARA CONFIGURAR EN NETLIFY:
# ============================================================================
#
# 1. Ve a tu sitio en Netlify Dashboard
# 2. Site settings > Environment variables  
# 3. Agrega estas variables (usa los valores reales, no los ejemplos):
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - NEXT_TELEMETRY_DISABLED=1
#
# 4. IMPORTANTE: También verifica que estén en tu netlify.toml
#
# 5. Redeploy tu sitio después de agregar las variables
#
# ============================================================================
# TROUBLESHOOTING:
# ============================================================================
#
# Si ves error "modo offline":
# 1. Verifica que las variables estén en Netlify UI
# 2. Verifica que la URL NO tenga trailing slash (/)  
# 3. Verifica que la ANON_KEY sea la correcta de Supabase
# 4. Revisa los logs de deploy en Netlify
# 5. Usa el script: npm run validate-env
#
# ============================================================================